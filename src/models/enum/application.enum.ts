//application enum
//0: applied but not seen
//1: applied and seen
//2: approved
//3: rejected
//4: accepted
enum ApplicationStatus {
    Applied = 0,
    Seen = 1,
    Approved = 2,
    Rejected = 3,
    Accepted = 4,
}

export default ApplicationStatus;

// Path: src/controllers/application/application.enum.ts
