import logging from "../../utils/logging";
import redisClient from "../../configs/redis";
import * as chatServices from "./_service.chat";
import * as awsServices from "../aws/_service.aws";
import * as chatImageServices from "../chatImage/_service.chatImage";

const connectSocket = (socket) => {};

export default connectSocket;
